---
const { article } = Astro.props;
---

<div class="w-[40%] topcard">
  <div>
    <div
      class="cardimg !bg-no-repeat !bg-cover h-[17rem] w-full"
      style={{ background: `url(${article.img})` }}
    >
    </div>
  </div>
  <div class="topcardbody mt-4 p-3">
    <h4 class="cardtitle text-2xl font-medium">{article.title}</h4>
    <p class="cardbody text-sm mt-2">
      {article.summary.substring(0, 540)}
      <span class="cardexpand text-mainColor">...see more</span>
    </p>
    <button class="rounded-full w-[60%] hidden h-[2.3rem] bg-mainColor mt-4"
      >Read Now</button
    >
  </div>
</div>
